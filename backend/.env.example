# Server Settings
PORT=5001
NODE_ENV=production
VERCEL=1

# MongoDB Connection (Yerel bağlantı için)
# MONGODB_URI=mongodb://localhost:27017/mern-todo-app

# MongoDB Atlas Bağlantısı (Canlı ortam için)
# MongoDB Atlas'tan alınan bağlantı URL'sini buraya ekleyin
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/your-database?retryWrites=true&w=majority

# JWT Secret (Güvenlik için benzersiz bir değer olmalı)
JWT_SECRET=create-a-secure-random-string-for-jwt-authentication

# OpenAI API Key (GPT özelliklerini kullanmak için gerekli)
# OpenAI web sitesinden alınan API anahtarını buraya ekleyin
OPENAI_API_KEY=sk-your-openai-api-key

# CORS Settings (Opsiyonel, ihtiyaca göre yapılandırılabilir)
# CORS_ORIGIN=https://your-frontend-domain.com 